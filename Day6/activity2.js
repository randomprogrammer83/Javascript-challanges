// array methods
// 1. push
  let arr=[1,2,3,4,5];

  console.log("beofre the push",arr);
  arr.push(6);

  console.log("after the push",arr);

  // 2. pop
  console.log("before the pop",arr);
  arr.pop();
  console.log("after the pop",arr);

  //shift
  console.log("before the shift",arr);
  arr.shift();
  console.log("after the shift",arr);

    // unshift
    console.log("before the unshift",arr);
    arr.unshift(6);
        console.log("after the unshift",arr);